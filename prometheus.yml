scrape_configs:
  - job_name: 'prometheus'
    scrape_interval: 1s

    static_configs:
      - targets: ['app']
        metrics_path: '/api/metrics'
        labels:
          service: 'prisma-metrics'
          group: 'production'
