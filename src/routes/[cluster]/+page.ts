import type { PageLoad } from './$types'

export const load: PageLoad = async ({ params, fetch, depends, route }) => {

    // const clusters = await (await fetch(`/api/clusters/all`)).json()

    // depends("clusters")

    // return {
    //     clusters
    // }

}


